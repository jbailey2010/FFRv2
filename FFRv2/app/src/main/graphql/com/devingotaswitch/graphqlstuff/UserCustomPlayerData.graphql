query GetUserCustomPlayerData {
  getUserCustomPlayerData {
    watchList
    notes
  }
}

mutation UpdateUserCustomPlayerData($watchList: String!, $notes: String!) {
  updateUserCustomPlayerData(watchList: $watchList, notes: $notes) {
    watchList
    notes
  }
}